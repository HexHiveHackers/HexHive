# Inspiration

Visual references for upcoming UI work. Each image is checked in alongside a one-line note about which feature it informs.

| File | For | Notes |
|---|---|---|
| `hackdex-username-setup.png` | `/me/setup` redesign | Hackdex's first-login username flow. Big "Finish setting up your account" heading, single-card form with `@`-prefixed input, real-time validation ("Username is available"), preview of the resulting profile URL, primary CTA, and a Sign-out escape. |
| `hackdex-username-setup-2.png` | `/me` redesign | Hackdex's account page. Card with avatar (large color circle + icon), "No name / @username" header, two-column field layout (Email full-width, Name + Username side-by-side, Website optional), small kebab menu in the corner, primary "Update profile" + secondary "Sign out". |

When a referenced design ships, leave the file here unless its feature is dropped — old inspiration is a useful lookback when the design needs to evolve.
